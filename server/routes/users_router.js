import { Router } from 'express';
var router = Router();

//Get users
router.get('/', (req, res) => {
    res.json( 'users')
})
//Post user

//Get user by Id

//Put user

//Delete user

export default router;
