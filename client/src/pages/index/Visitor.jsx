import ArticleList from "../../components/articleContainer/ArticleContainer";

const Visitor = () => {
    return (
        <div>
            <ArticleList />
        </div>
    );
};

export default Visitor;
