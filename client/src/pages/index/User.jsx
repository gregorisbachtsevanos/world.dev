import ArticleList from "../../components/articleContainer/ArticleContainer";

const User = ({ user }) => {
    return (
        <div>
            {/* <p>{user.username}</p> */}
            <ArticleList />
        </div>
    );
};

export default User;
