import ArticleList from "../../components/articleList/ArticleList";

const User = ({ user }) => {
    return (
        <div>
            {/* <p>{user.username}</p> */}
            <ArticleList />
        </div>
    );
};

export default User;
